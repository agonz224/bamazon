CREATE TABLE `bamazon`.`bamazon market` (
  `item_id` INT NOT NULL,
  `product` VARCHAR(45) NOT NULL,
  `department` VARCHAR(45) NOT NULL,
  `price` INT NOT NULL,
  `stock_quantity` INT NOT NULL,
  PRIMARY KEY (`item_id`));
